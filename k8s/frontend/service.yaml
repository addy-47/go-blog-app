apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: ci-cd
spec:
  type: LoadBalancer # Use LoadBalancer to expose the frontend externally on GKE
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: frontend